def count_in_list(lst, element):
    """
    Returns the count of occurrences of element in lst.

    Parameters:
    lst (list): The list to search.
    element (any): The element to count in the list.

    Returns:
    int: The count of occurrences of the element in the list.
    """
    return lst.count(element)
